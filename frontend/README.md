# Graline

## Setup Instructions
1. Run `npm install`
2. Copy `.env.example` to `.env` and configure your database settings.
3. Run `npx prisma db push` and `npx prisma db seed` to initialize the database.
4. Run `npm run dev` to start the development server.
